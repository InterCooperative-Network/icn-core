groups:
  - name: icn-alerts
    rules:
      - alert: NodeDown
        expr: up == 0
        for: 1m
        labels:
          severity: critical
        annotations:
          summary: "ICN node down"
          description: "The node {{ $labels.instance }} is not responding."
