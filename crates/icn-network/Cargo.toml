[package]
name = "icn-network"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true

[dependencies]
icn-common = { path = "../icn-common", version = "0.1.0" }
libp2p = { version = "0.53", optional = true }

[features]
default = []
with-libp2p = ["dep:libp2p"]
