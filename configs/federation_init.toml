# Template configuration for initializing a new federation
# Customize values as needed for your deployment
http_listen_addr = "0.0.0.0:7845"
storage_backend = "sqlite"
storage_path = "./icn_data/federation.sqlite"
# Node private key will be generated by `icn-cli federation init`
bootstrap_peers = []
federation_name = "icn-demo"
